# ─── SyntaAI SAP Security MCP Server - OData Edition ─────────────
# Copy this file to .env and fill in your values
# cp .env.example .env

# ─── SAP Connection ──────────────────────────────────────────────
# Your SAP system URL (HTTPS recommended)
SAP_BASE_URL=https://your-sap-host:44300
SAP_CLIENT=100
SAP_USER=ODATA_COMM_USER
SAP_PASSWORD=your-password
SAP_LANGUAGE=EN

# ─── OData Mode ──────────────────────────────────────────────────
# "standard" = Use SAP standard OData APIs (limited security coverage)
# "custom"   = Use your custom OData security service (recommended)
ODATA_MODE=custom

# Custom OData service path (used when ODATA_MODE=custom)
# Change this to match YOUR custom OData service name
ODATA_SERVICE_PATH=/sap/opu/odata/sap/ZSECURITY_SRV

# ─── SSL / Security ─────────────────────────────────────────────
# Set to false for self-signed certs (not recommended for production)
VERIFY_SSL=true

# HTTP timeout in seconds
HTTP_TIMEOUT=30

# ─── MCP Authentication ─────────────────────────────────────────
# Secret key for MCP session management
MCP_SECRET_KEY=change-this-to-a-random-string
